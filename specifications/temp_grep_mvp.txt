01b-tech-stack.md:2711: ### Firebase Data Connect + SQLAlchemy — Division of Responsibility

01b-tech-stack.md:2713: *(Addendum: consolidating the Firebase Data Connect decision with environment architecture)*

01b-tech-stack.md:2715: Firebase Data Connect is scoped per Firebase project — meaning dev, staging, and

01b-tech-stack.md:2716: prod each have their own Data Connect instance, GraphQL schema, and generated

01b-tech-stack.md:2717: Flutter SDK. Schema changes to Data Connect are promoted through environments

01b-tech-stack.md:2724: | Flutter client → Cloud SQL (CRUD) | Firebase Data Connect (GraphQL) | Schema definition, type-safe Flutter SDK generation, simple mutations and queries from frontend clients |

01b-tech-stack.md:2737: ### Updated Stack Summary — Environments + Firebase Data Connect Added

01b-tech-stack.md:2745: | Flutter client queries | Firebase Data Connect (GraphQL, type-safe SDK) | ✅ Added |

02-architecture.md:433:    │  Per-tenant     │ │                  │ │  Data Connect    │

07b-architecture-pivot-audit.md:1: # Architecture Pivot Audit: Pure API (FastAPI) vs Hybrid (FDC)

07b-architecture-pivot-audit.md:4: Transitioning from a hybrid architecture (where Flutter communicates directly with the database for CRUD via Firebase Data Connect) to a **Pure API-only approach** (where Flutter communicates exclusively with Python FastAPI for *all* operations) is a highly strategic move, especially when utilizing AI agents and TDD.

07b-architecture-pivot-audit.md:10: | **Industry Best Practices** | **Significantly Improved.** The hybrid approach risks creating a "fat client" where business rules leak into the frontend. The Pure API approach adheres to classic 3-tier architecture (Client -> App Server -> Database). It prevents vendor lock-in (FDC is highly proprietary) and uses universally understood standards (REST/GraphQL via Python). |

07b-architecture-pivot-audit.md:11: | **App Security** | **Significantly Improved.** Security is centralized. In the hybrid model, RBAC and row-level security are split between FDC `@auth` rules and FastAPI code. In a Pure API model, 100% of authentication (Firebase JWTs) and authorization (RBAC) happens in your Python middleware. There is a single, impenetrable gatekeeper (FastAPI) backed by Firebase App Check. |

07b-architecture-pivot-audit.md:12: | **Maintainability & TDD** | **Massively Improved.** Because you specified Test-Driven Development (TDD) and AI hand-offs, a Pure API is superior. You can easily write `pytest` suites to mock and test FastAPI endpoints. Testing Firebase Data Connect requires complex emulator setups. Furthermore, having a single language (Python) for *all* backend logic ensures your AI agents don't get confused juggling FDC GraphQL schemas versus SQLAlchemy models. |

07b-architecture-pivot-audit.md:13: | **Dev Time & Cost** | **Slight Initial Increase / Long-term Decrease.** FDC's main selling point is saving time by auto-generated frontend SDKs. By removing it, the AI agents will have to manually write FastAPI CRUD routers, Pydantic schemas, and Flutter HTTP/Dio clients. *However*, because AI agents excel at writing boilerplate code, this penalty is negligible. Long-term, dev time decreases because debugging a single API layer is much faster than debugging a hybrid split. Cost also decreases as you don't need to provision or monitor the FDC service. |

07b-architecture-pivot-audit.md:19: If we lock in this decision, the following artifacts must be surgically updated to purge Firebase Data Connect and establish FastAPI + SQLAlchemy as the sole backend authority.

07b-architecture-pivot-audit.md:23:    * **Action:** Remove all references to Firebase Data Connect (FDC). 

07b-architecture-pivot-audit.md:26:    * **Action:** Ensure any notes relying on FDC auto-generation are removed and replaced with standard API endpoint development tasks.

07b-architecture-pivot-audit.md:29: Currently, all `06-*.md` files are written using FDC GraphQL syntax with FDC-specific directives (e.g., `@auth(level: USER)`, `@ref`, `@default`).

07b-architecture-pivot-audit.md:31:    * **Action:** Strip all FDC directives.

07b-architecture-pivot-audit.md:34:    * **Action:** Ensure the indexes are defined as SQLAlchemy/Alembic migration instructions rather than FDC metadata.

07b-architecture-pivot-audit.md:38:    * **Action:** Update the Sprints. Remove references to "Defining FDC Schemas" or "Generating FDC SDKs".

07b-architecture-pivot-audit.md:41:    * **Action:** Update the exact task checklists to reflect the new API-only flow (e.g., creating Alembic migrations instead of pushing FDC schemas).
